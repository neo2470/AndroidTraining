<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- the theme to applied to the application or activity -->
    <style name="CustomActionBarTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/ActionBarTabStyle</item>
        <item name="android:actionMenuTextColor">#ff0000</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

    <!-- ActionBar style -->
    <style name="MyActionBar" parent="android:Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:titleTextStyle">@style/ActionBarTitleStyle</item>
    </style>

    <!-- ActionBar title style -->
    <style name="ActionBarTitleStyle" parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#0000ff</item>
    </style>

    <!-- ActionBar Tab text style -->
    <style name="ActionBarTabStyle" parent="android:Widget.ActionBar.TabText">
        <item name="android:textColor">#00ff00</item>
    </style>
</resources>